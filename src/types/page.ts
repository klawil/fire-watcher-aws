export interface PageConfig {
  title?: string;
  navTitle?: string;
  hasAudio?: boolean;
  centerAll?: boolean;
  requireAuth?: boolean;
  requireAdmin?: boolean;
  fluid?: boolean;
  containerClass?: string;
}
